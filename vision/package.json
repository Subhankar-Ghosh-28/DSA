{
  "name": "dsa-study-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "Local DSA study dashboard with progress tracking and a C++ runner",
  "scripts": {
    "start": "node runner-server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check runner-server.js && node --check scripts/script.js && node --check scripts/topic.js && node --check scripts/runner-page.js"
  }
}
